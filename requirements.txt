# Root requirements for the Streamlit Cloud deployment (streamlit_app.py).
# The desktop backend deps live in python/requirements.txt.
streamlit>=1.30
streamlit-autorefresh>=1.0.1
pandas>=2.0
httpx>=0.28.0
cryptography>=42.0.0
# Persist the SQLite DB to Drive — Streamlit Cloud's disk is wiped on restart.
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0
